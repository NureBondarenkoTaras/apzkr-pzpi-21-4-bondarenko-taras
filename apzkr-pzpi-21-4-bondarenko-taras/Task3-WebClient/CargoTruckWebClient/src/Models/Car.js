export class Car {
    constructor(id, numberMia, brand, loadCapacity) {
      this.id = id;
      this.numberMia = numberMia;
      this.brand = brand;
      this.loadCapacity = loadCapacity;
    }
  }