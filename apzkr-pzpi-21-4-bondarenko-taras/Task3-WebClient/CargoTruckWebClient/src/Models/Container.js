export class Container {
    constructor(id, name, type, loadCapacity, length, height, width, status) {
      this.id = id;
      this.name = name;
      this.type = type;
      this.loadCapacity = loadCapacity;
      this.length = length;
      this.height = height;
      this.width = width;
      this.status = status;
    }
  }